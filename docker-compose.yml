services:
  konvertio:
    build: .
    image: konvertio:latest
    ports:
      - "8000:8000"
    environment:
      KONVERTIO_MAX_UPLOAD_MB: "100"
      KONVERTIO_ALLOW_URL_FETCH: "true"
      KONVERTIO_MCP_ENABLED: "true"
    restart: unless-stopped
